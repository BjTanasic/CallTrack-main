CallTrack-main/          ← GitHub repo root
├── .github/
│   └── workflows/
│       └── deploy.yml   ← stays here (root level)
└── CallTrack-main/      ← your project subfolder
    ├── package.json
    ├── src/
    ├── public-clean/
    └── ...
